export const cardKey = (key: string) => `card.${key}`;

export const commonKey = (key: string) => `common.${key}`;

export const stageKey = (key: string) => `stage.${key}`;

export const stuffKey = (key: string) => `stuff.${key}`;
